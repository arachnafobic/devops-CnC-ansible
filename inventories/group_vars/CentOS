---

service_ssh_name: sshd

# Used during initialize play
install_epel:
  - epel-release
install_init:
  - python-pip
  - nano
install_pip_init:
  - passlib

# Used during main play
pkgs_beats:
  - filebeat
  - metricbeat
  - auditbeat
  - packetbeat
