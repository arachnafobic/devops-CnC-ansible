general:

- remove ALL code for CloudLinux support

roles:

- common
  - systemd-resolvd bypass needs hardening (ubuntu bionic)

- phpmyadmin
  - make webroot dynamic (ubuntu/centos)

- sensu
  - move rabbitmq user/vhost setup out of rabbitmq role to sensu (ubuntu/centos)

- graphite-web
  - installs allright, 502's tho (centos)

- froxlor (ubuntu/centos)
  - replace a few skin templates with custom versions to hide unneeded options/text and add a phpmyadmin link to the admin user's sidebar
  - add an optional switch to pre-populate froxlor with a user that has a working default wordpress site
